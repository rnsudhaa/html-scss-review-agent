// Intentionally mirrors examples/good/Card/index.ts for parity.
// The bad example still ships a default export that violates several REACT-* rules.
export { Card } from './Card';
export type { CardProps, CardItem } from './Card';
export { default } from './Card';
